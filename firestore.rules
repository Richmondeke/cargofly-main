rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow all read/write for development testing
    // TODO: Restore proper security rules before production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
